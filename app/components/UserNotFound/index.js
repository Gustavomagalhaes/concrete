import styled from 'styled-components';

const UserNotFound = styled.span`
  font-family: Raleway;
  font-size: 40px;
  font-weight: normal;
  font-style: normal;
  font-stretch: normal;
  line-height: normal;
  letter-spacing: normal;
  color: #ac53f2;
`;

export default UserNotFound;
